/** Applies response headers across common response adapters (`setHeader`, `header`, `set`). */
export declare function applyHeaders(res: Record<string, unknown>, headers: Record<string, string>): void;
